interface HeroProps {}

function Hero(props: HeroProps) {
  return (
    <section>
      <div>{/* Image */}</div>
      <h1>{/* Intro */}</h1>
      <p>{/* Desc */}</p>
    </section>
  );
}

export default Hero;
